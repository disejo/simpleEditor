{
  "name": "simpleeditor",
  "version": "1.0.0",
  "description": "Editor de código para estudiantes para practicar HTML, CSS y JS",
  "main": "server.js",
  "scripts": {
    "build:css": "tailwindcss -i src/styles.css -o public/styles.css --minify",
    "start": "npm run build:css && node server.js",
    "dev": "nodemon --watch src --watch public --ext css,js,html --exec \"npm run build:css && node server.js\""
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "nodemon": "^3.0.1",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.0"
  }
}